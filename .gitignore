# Binarios compilados (plataforma-dependientes)
analizador_lexico
analizador_sintactico
analizador_lexico.exe
analizador_sintactico.exe
compilador
compilador.exe

# Archivos generados por Bison y Flex (se regeneran con `make`)
src/lex.yy.c
src/parser.tab.c
src/parser.tab.h
src/parser.output

# Archivos de depuración y temporales
*.o
*.out
.vscode/
__pycache__/
*.pyc

# Artefactos del build del instalador de Windows
# (Python embebido: se descarga/descomprime; el instalador: lo genera Inno Setup)
installer/python/
installer/Output/
installer/*.zip

# Herramientas locales descargadas (toolchain portable, WinAPE, Python)
# Solo se versiona el script que las descarga.
tools/*
!tools/descargar_winape.ps1
!tools/descargar_winape.sh
